from SM import SM

class ObstacleDetector(SM):
    startState = 'nothing'  # you can change this to something else if you want

    def getNextValues(self, state, inp):
        if inp < #write ranges here basil:
            # do specific staff here
        # continue the same thing here with 5 more elifs.


# the objects of Obstacle Detector should run inside a while True.